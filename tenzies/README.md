# README

1. Decide the design and create the basic html and css. 
2. In App component, create a state called dices, which is an array of 10 objects. Each of the object has these keys: id, value, isHeld
3. Create another component Dice, which renders each of the button
4. Map over the dices state, pass the props and render the Dice component
5. Crate a generatAllDice function, and trigger it on first render and everytime when Roll button is clicked. 


